import * as React from "react";
import { FiMenu } from "react-icons/fi";

export const ButtonToggleMenu: React.FC = () => {
  return (
    <button className="text-xl">
      <FiMenu />
    </button>
  );
};
